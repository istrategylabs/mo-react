import styled from 'styled-components';
import { TEXT } from '../styles/colors';

export default styled.div`
  h1 {
    font-size: 5vmin;
    font-weight: normal;
    color: ${TEXT};
  }
  p {
    font-size: 1rem;
  }
`;
